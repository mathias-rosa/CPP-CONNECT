# Copy this file to .env and fill in the values

# MongoDB connection (overridden by docker-compose to use the local mongo service)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/test

# Flask mail credentials (Mailjet)
EMAIL_USER=your_mailjet_api_key
EMAIL_PASS=your_mailjet_secret_key
